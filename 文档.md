# 当前唯一运行版本

当前活动版本是仓库根目录代码，对应 GitHub `main@914fea0`。项目使用 RealSense 扫描可见胶带，生成一条中心线和逐点曲面法向姿态，再由 Franka 原始控制接口执行正向、反向擦拭并回位；不启用 MoveIt。

## 运行入口

```bash
cd /home/pnp/curve_wipe_demo

# 只扫描、规划和预检查，不移动机械臂
./run_full_wipe.sh --target-color white

# 实机全流程：检查/回到初始位姿 → 扫描 → 规划 → 正向擦拭 → 反向擦拭 → 回位
./run_full_wipe.sh --execute --target-color white
```

支持的目标颜色包括 `red`、`orange`、`yellow`、`lime`、`green`、`cyan`、`blue`、`violet`、`purple`、`magenta`、`pink`、`brown`、`gray`、`white` 和 `black`。画面外或被遮挡的部分不纳入本次区域。

完整参数和执行顺序见 [端到端运行说明.md](端到端运行说明.md)，版本验证记录见 [VERSION_1.md](VERSION_1.md)。

## 当前控制参数

- 默认只规划一条当前可见胶带中心线。
- 擦拭中心是夹爪中心沿夹爪局部 Z 轴前移 15 mm 的点。
- 每个轨迹点根据扫描曲面法向生成空间姿态，夹爪 Z 轴与曲面法向对齐。
- 空中接近、离面和回位不启用力传感；接触后目标法向力为 1 N，并使用导纳控制。
- 合力保护阈值 8 N，法向力保护阈值 8 N，接触力矩保护阈值 1.5 Nm。
- 正反向切换保持 0.5 s；法向平滑尺度为 20 mm，用于抑制深度/PCA 噪声造成的姿态突变。
- 默认速度倍率 1.5，擦拭速度约 4.5 mm/s。

## 失败处理

出现 FCI、关节、跟踪、传感器、超力或超时故障时立即停止并记录日志，不自动清故障、不盲目回位、不复用旧扫描计划。确认 Desk/FCI 恢复且机械臂处于安全空闲状态后，再从入口重新执行。

## 版本管理

仓库只保留根目录这一套活动代码。旧版本副本已从工作树移除，但仍可通过 Git 历史查看；不要再从旧路径运行脚本。
