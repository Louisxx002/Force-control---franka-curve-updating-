# 当前使用：原 Demo

按当前选择，已停止 MoveIt 规划服务，继续使用原有 RealSense 扫描、局部轨迹规划和独立力控擦拭流程。MoveIt 扩展保留备用，默认不启用。本次仅切换软件流程，没有移动机械臂。

对比文档：[原 Demo 与 MoveIt 方案对比](原Demo与MoveIt方案对比.md)。

常用入口：`./run.sh preview`、`./run.sh autowipe`；后者只做几何预览。当前不传 `--depth-scene` 或 `--motion-config`，也不启动 `planning`。

以下保留原操作说明；末尾 MoveIt 章节为历史开发记录。

实时预览窗口：左侧彩色画面，右侧深度图，不控制机械臂（需运行下方命令启动）。
以后可在终端运行：
cd /home/pnp/curve_wipe_demo
./run.sh preview
- R：切换红色痕迹高亮
- S：保存截图
- Q / Esc：关闭预览、释放相机
调整到整条胶带清晰可见后，关闭预览，再让我重新扫描。

擦拭结束后的自动返回：
- 正常完成往返擦拭后，先沿法向离开曲面，再经过本次预接触点，回到本次启动时的悬空位姿。
- 固定姿态 execute 和曲面姿态 adaptive_execute 两个入口均已加入。
- 返回期间继续监测力、位姿跟踪和关节状态；确认位置误差小于1mm、姿态误差小于0.5°后才标记完成。
- 发生故障时仍停止，不自动清故障或盲目回位。
- 下一轮应重新采集图像和位姿，确认工件、胶带没有改变。

变姿态擦拭前的重力标定：
详细说明见 [重力标定操作.md](重力标定操作.md)。
采样命令：`./run.sh calibrate_payload collect`
脚本只读取，不移动机械臂；默认16个拟合姿态、6个独立验证姿态。
每次手动调整悬空姿态、松手停稳后按Enter采样，输入q可保存退出。
# 2026-09-12：力传感器单位修正

当前 PNP75 的原始输出经用户确认为 kgf / kgf·m。读取接口已统一乘 9.80665，
对外输出 N / Nm。此前擦拭日志的 N/Nm 标注有误，不能把旧“1 N”目标视为实际 1 N。
旧标定数据已另存 SI 副本，重算验证残余力 RMS 为 0.656 N，尚未通过变姿态标定。
当前没有生成可执行重力标定配置，也未启动新擦拭。详细结果与重采命令见
[重力标定操作.md](重力标定操作.md)。

## 自动识别与规划入口

`./run.sh autowipe` 自动扫描、检测红色区域、筛选轨迹并生成悬空接近方案，目前只做规划预览，不移动机械臂。无需手工填写像素范围。

完整说明、报告位置和后续实机闭环工作见 [AUTOWIPE.md](AUTOWIPE.md)。

自动规划的 MoveIt 只读审查已接入：`./run.sh autowipe --motion-config config/motion_scene.example.json`。示例配置需要按现场核对，当前规划服务尚未启动；即使采样审查通过也不会自动发出运动。详见 [AUTOWIPE.md](AUTOWIPE.md)。

## MoveIt 续接进度

规划服务此前通过 `./run.sh planning` 启动在 `/autowipe`，现已停止，本地审查改用 `config/motion_scene.local.json`。真实服务的历史姿态 FK 核对已通过，轨迹执行仍关闭。

当前阻塞已变为真实碰撞场景缺失。新增 `config/scene_measurements.template.json` 待测量表及 `./run.sh load_scene` 导入入口。详细命令与剩余执行链路见 [AUTOWIPE.md](AUTOWIPE.md) 的“本机规划服务已启动”章节。尚未完成整条现场路径验证或自动实机闭环。

## 无障碍实验：接近距离参数

固定姿态 `execute` 和变姿态 `adaptive_execute` 的接近上限已由 120 mm 改为默认 500 mm，均支持 `--max-approach-mm`。这是到预接触点的三维距离，不是曲面起伏上限。

```bash
# 离线预检查，不连接硬件；将占位路径替换为本次候选计划
./run.sh execute --plan <本次计划路径> --max-approach-mm 500
```

可传 `--max-approach-mm 120` 恢复旧门槛。原有接近速度、离面调零、曲面起伏、接触搜索、超力和跟踪检查保留；变姿态仍需当前安装的有效重力标定。本次未启动实机运动。

## 悬空力阈值调整

按用户要求，固定姿态 `execute` 的悬空异常接触阈值从 0.3 N 调整为 0.75 N，覆盖悬空接近、停稳及返回阶段。预检查与新运行日志记录该阈值。慢速搜索的接触判定仍为 0.3 N，擦拭目标仍为 1 N，总力/力矩保护不变。本次仅修改参数，未自动重启擦拭。

## 2026-09-13：指定固定扫描初始位姿（待采集）

用户指定当前能看到整个曲面的姿态作为后续每轮擦拭前的扫描初始位姿。流程应为：检查当前状态与回位条件 → 回到记录的扫描位姿 → 重新采集 → 规划 → 擦拭。这里是回到参考位姿，不是编码器标零。

本次尝试读取时 FCI 拒绝连接：`Connection to FCI refused ... enable FCI mode in Desk`。因此尚未保存当前位姿数值，也未启用自动回位；不能用之前的历史扫描替代本次指定姿态。开启 FCI 并保持当前姿态后，应记录 O_T_EE、七个关节角和 F_T_EE，核实工具安装没有改变。故障、接触状态或回位条件不满足时，不直接从任意位置直线回位。

### 初始位姿已保存（2026-09-13 10:33:11）

开启 FCI 后已连续两次读取并通过静止一致性检查。当前参考文件为 [config/scan_initial_pose.json](config/scan_initial_pose.json)，原始记录归档至 [output/initial_pose_20260913_103311/reference.json](output/initial_pose_20260913_103311/reference.json)。

基坐标下 EE 位置：X=418.074 mm、Y=263.699 mm、Z=421.678 mm。文件同时保存完整旋转矩阵、七关节角、F_T_EE 与两次原始状态。整曲面可见性按用户当前现场确认记录；本次未占用正在运行的相机预览，也未移动机械臂。

后续每轮以该参考位姿作为扫描起点，回位后重新采集。当前仅完成记录，自动回位尚未接入执行入口；原擦拭结束逻辑仍返回“本次启动悬空位姿”，不会自动改为上述固定参考。

## 2026-09-13：扫描初始位姿准备入口已接入

```bash
# 只读检查是否在保存的初始位姿，不移动、不占用相机
./run.sh prepare_wipe

# 关闭相机预览后运行：必要时回位 → 新扫描 → 整条红条规划
./run.sh prepare_wipe --execute
```

`prepare_wipe --execute` 只授权悬空回位与采集，不启动接触擦拭或夹爪开合。今后每轮采用这个准备入口；原 `execute` / `adaptive_execute` 不会在收到已有计划后擅自回位，以免使计划对应的采集姿态失效。

已在参考位姿 1 mm / 0.5° 容差内时跳过运动，直接重新采集。否则先采集当前位置的曲面数据，沿原 `reposition` 路线在悬空位置调整姿态，再移动到参考位置。沿用该回位模块的 25° 姿态变化、180 mm TCP 平移、相对观测曲面至少 100 mm 离面条件。它们是回位条件，与擦拭入口的 500 mm 接近参数分别管理；不支持从接触状态或任意远处盲目回位。

回位期间检查状态、关节2、位姿跟踪、循环超时及六轴力变化。姿态变化期间使用原回位模块的合力变化 2 N / 力矩变化 0.3 Nm 门槛，不把固定姿态擦拭的 0.75 N 法向阈值直接用于变姿态。到位后核对位置/姿态与 F_T_EE，扫描前后再次确认参考位姿。失败则停止，不自动清故障，不复用回位前图像。

本机只读验证：[initial_pose_check_20260913/report.json](output/initial_pose_check_20260913/report.json)。结果 at_reference，位置误差 0.00247 mm，姿态误差 0.000503°。119 项测试通过；本次未做实机回位运动验证。

上文“自动回位尚未接入”描述保存参考时的历史状态；现在软件准备入口已接入，回位运动实测仍待完成。完整红条计划仍为预览，固定姿态约束与多条路径间的接触执行尚未接通。

## 2026-09-13：固定姿态曲面筛选阈值调整

曲面起伏指路径点沿保持不变的工具法向投影后的最大值减最小值，不是工件距离基座的高度；默认从10 mm改为20 mm。法向夹角指局部曲面外法向与保持不变的工具外法向夹角，默认从15°改为25°。工具仍保持固定姿态，不会因此自动贴合曲面。

`execute` 新增 `--max-surface-height-mm 20 --max-normal-angle-deg 25`。默认值同时用于新的几何规划筛选；新执行日志和预检查记录实际使用阈值。悬空0.75 N、目标力1 N、接触搜索和跟踪保护没有改变。

最新保存扫描的5条完整路径中，2、3、4号通过新几何门槛（每条约165 mm）；0、1号仍超过25°。复查见 [relaxed_audit.json](output/wipe_return_trial_20260913/full_strip/relaxed_audit.json)。历史规划文件不覆盖，仍保持不可执行标记；本次只调整参数及复查，未启动擦拭，也没有把3条通过当作全部5条覆盖完成。

## 端到端新入口

已新增 `./run_full_wipe.sh`（默认只预检查）和 `./run_full_wipe.sh --execute`（回位、扫描、全部路径擦拭、逐条回位）。完整使用方式、参数和失败行为见 [端到端运行说明.md](端到端运行说明.md)。本入口默认30 mm起伏、45°法向夹角、500 mm接近；不改变独立旧入口默认值。本次仅完成软件与离线验证，未运行端到端实机擦拭。

## 最新：擦子中心沿单条红条中心线，速度提高50%

端到端入口 `./run_full_wipe.sh --execute` 现默认1条中心线、`--speed-scale 1.5`。通过夹爪中心上方 15 mm 的擦拭中心反算末端位置。最新完整规划长约164.7 mm；沿线速度4.5 mm/s，悬空接近与返回15 mm/s。其它分阶段速度和历史默认值的区别见 [端到端运行说明.md](端到端运行说明.md)。本次只修改与离线验证，没有运动。

## 夹爪中心与擦子前端参考点（2026-09-13）

当前擦拭轨迹使用 `config/eraser_tcp_candidate.json` 中的
`T_EE_L_WIPE_CENTER`，它定义为夹爪中心沿本地 Z 轴正方向 15 mm 的擦拭点。

代码现已把两个参考点分开：`T_EE_L_GRIP_CENTER` 保存夹爪中心，
`T_EE_L_WIPE_CENTER` 保存擦拭中心。规划以擦拭中心生成曲面轨迹，再通过该变换反算夹爪/末端位姿。

当前 `active_contact_reference` 已切换为 `wipe_center`：X/Y 与夹爪中心重合，Z 方向相差 15 mm。规划输出会记录 `contact_reference`、`front_contact_calibrated` 和 `T_ee_grip_center`。如需改成真正的擦子端部，再填写 `T_EE_L_ERASER_FRONT` 并选择 `eraser_front`。

## PNP75 力控逻辑调整（2026-09-13）

`execute` 现在分为两个阶段：

- 悬空接近、离面和回位：不读取力传感器样本，使用位姿跟踪、关节、循环超时和机器人状态保护。
- 到达预接触位置后：重新采集一秒静止传感器基线；进入慢速接触、建立力和擦拭。每个 10 ms 控制周期使用 RealSense 估计的局部曲面法向重建 TCP 姿态，夹爪 TCP +Z 指向曲面内侧（`TCP_Z = -normal_out`），再通过工具标定旋转换算末端姿态；法向力误差通过有界虚拟导纳转换为法向位移修正：`v_n = clip(0.0015 * (F_n - 1 N), ±0.002 m/s)`，控制目标为1 N。

接触阶段仍检查合力8 N、法向力8 N、力矩0.5 Nm、反向反力1.5 N、接触丢失2 s和视觉穿透12 mm。接触检测门槛为0.30 N，必须连续保持1 s才确认接触；接触保持门槛为0.20 N。悬空0.75 N阈值不再参与空中控制，因为空中不读取力样本。串口读线程仍在进入执行器时打开，接触交接处才要求新鲜帧和基线；这次修改解决了悬空移动期间的 PNP “No fresh complete serial frame” 触发，不会用旧帧冒充当前力。

擦拭结束后，端到端调度器会先核对本轮起始位姿；若与保存的扫描初始位姿只相差不超过 5 mm / 5°，会在空中执行一次受限的参考位姿校正并重新核对，解决毫米级 CartesianWaypoint 末端误差导致的误报。偏差超出该范围不会盲目校正。

## 白色胶带目标模式（2026-09-13）

端到端入口支持 `--target-color white`。白色胶带的反光区域可能缺少深度，规划器使用胶带上下相邻的有效曲面点做局部平面重建，并对中心线做轻度平滑；局部拟合残差超过 4 mm 或整条轨迹不连续时仍拒绝接触执行。一次成功实机记录见 [cycle_white_tape_wipe_run2_20260913/cycle_report.json](output/cycle_white_tape_wipe_run2_20260913/cycle_report.json)。

同一入口支持 `--target-color black`。黑色胶带使用非边界的暗色细长连通区域，并允许斜向主轴轨迹；本次成功实机记录见 [cycle_black_tape_wipe_run_20260913/cycle_report.json](output/cycle_black_tape_wipe_run_20260913/cycle_report.json)。

最近一次诊断记录 [pnp_diagnostic_20260913.json](output/pnp_diagnostic_20260913.json)：25.0 s 收到25,028帧、0个坏帧，读线程正常，最大观测间隔18.8 ms，未出现陈旧样本。此前中止日志 [wipe.json](output/cycle_20260913_105834/lane_00/wipe.json) 发生在接近阶段，未进入接触擦拭；当时实际串口读线程统计未被写入，因此不能证明是传感器物理断线。现在如果接触阶段再出现陈旧帧，程序仍会立即停止并记录错误，不会降级为无传感器力控。

## PNP75 诊断与最近一次端到端中止

PNP75 串口诊断正常：25秒收到25,028帧、坏帧0、读线程正常、最大观测间隔18.8ms。最近一次端到端运行的首个中止不是PNP物理断线，而是软件时钟错误：接触前一秒基线采集后没有重置100Hz控制循环时间戳，首个控制周期被判为1.01秒停顿。现已修复并通过135项测试。

修复后重试尚未进入擦拭；上一次中止残留的机械臂当前状态为Idle/无故障，但末端位于X=448.361mm、Y=429.224mm、Z=80.124mm，距保存扫描初始位姿约381mm。相机在该位置看不到红条，所以程序拒绝用旧曲面观测盲目生成回位路径。下一步应在确认无障碍的前提下把机械臂回到 [scan_initial_pose.json](config/scan_initial_pose.json)，再重新扫描；不能直接复用中止前的计划。

## 2026-09-13 回位故障记录

端到端重试后，机器人在空中回位过程中触发 Franka 保护：`cartesian_motion_generator_acceleration_discontinuity`，当前 `robot_mode=4`，运动已停止，未进入擦拭。故障由新增回位代码第一帧直接切换姿态造成，不是 PNP75 串口问题。已改为使用 Slerp 平滑姿态插值；未在故障状态下再次发送运动命令。

下一步必须先在 Desk 清除该故障、确认 FCI/机器人回到 Idle，再重新做只读状态检查。不要重复运行 `--execute`，也不要手动清除日志后继续；故障恢复后应先用低速单独验证回位，再进入整条擦拭。
