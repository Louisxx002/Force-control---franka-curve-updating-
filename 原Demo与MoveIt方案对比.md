# 原 Demo 与 MoveIt + RealSense 方案对比

更新日期：2026-09-12。本文依据当前项目代码和本机验证结果整理。

**当前回到原 Demo 路线。** MoveIt 规划服务已停止，后续使用原来的相机预览、扫描、局部路径规划和独立擦拭入口。MoveIt 扩展代码及验证记录保留，作为备用方案，不是当前流程的必需依赖。本次切换没有启动机械臂运动。

## 1. 两种方案分别解决什么问题

原 Demo 的重点是：用 RealSense 找到需要擦拭的局部曲面，在已满足接近条件的位置，以力反馈完成一段往返擦拭。

MoveIt 扩展的重点是：在原 Demo 的视觉与力控能力之外，补充机械臂整臂的可达性与碰撞路径审查。现场环境使用 RealSense 点云构建，不需要桌面、工件或支架的 CAD；机械臂自身使用已安装的 FR3 模型，末端附件需要简单实测包络。

MoveIt 不能代替红色目标识别、曲面估计或接触力控制。它也不会仅因为点云导入成功，就自动完成擦拭。

## 2. 流程对比

### 原 Demo：当前使用

相机预览、调整可见范围 → 采集图像/深度及静止机器人位姿 → 识别红色区域、估计局部曲面 → 检查选定片段与接近条件 → 使用独立执行入口完成力控往返擦拭 → 正常完成后离面、返回本次启动悬空位姿。

其中 `autowipe` 是自动识别和几何规划预览入口，目前不会自动接到实机执行。原 Demo 的实机能力来自独立 `execute` / `adaptive_execute` 入口，不能把自动预览计划改一个状态字段就交给机械臂。

### MoveIt + RealSense：备用开发路线

采集同一份图像/深度与机器人位姿 → 原 Demo 生成曲面路径和接近方案 → 全图有效深度变换到机器人基坐标 → 生成观测体素碰撞几何并导入 MoveIt → 使用保存的关节状态检查模型及接近路径。

预期后续再接入：按已验证的关节轨迹接近 → 到位复扫 → 原 Demo 力控擦拭 → 验证退回路径并执行。

**后半段目前尚未接通。** 现有 MoveIt 服务关闭了运动执行能力。

## 3. 能力对照

| 项目 | 原 Demo | 当前 MoveIt 扩展 |
|---|---|---|
| 现场 CAD | 不需要 | 不需要，环境来自 RealSense 点云 |
| 机器人模型 | 通过现有机器人接口运动，应用层未做整臂场景碰撞规划 | 使用本机 FR3 URDF/SRDF 进行运动学和碰撞计算 |
| 红色目标识别 | 已有；不能区分可擦痕迹、胶带和红色标识 | 沿用原 Demo |
| 曲面估计 | 深度点和局部法向，深度空洞不强行连接 | 沿用原 Demo |
| 接近方案 | 几何接近方案与执行入口的距离/姿态限制 | 已接笛卡尔路径服务及采样审查；完整现场路径尚未通过 |
| 整臂避障 | 未实现基于环境模型的完整碰撞验证 | 可调用模型碰撞检查，但当前缺末端附件几何 |
| 障碍绕行 | 未实现自动全臂绕行 | OMPL 已加载，失败后自动切换 OMPL 绕行的逻辑尚未接入 |
| 力控擦拭 | 已有固定姿态执行及变姿态软件入口 | 仍需使用原 Demo 的力控模块，MoveIt 不直接替代 |
| 正常结束回位 | 独立擦拭入口已有离面与返回逻辑 | 尚未把整臂验证与返回执行串起来 |
| 自动连续闭环 | 尚未完成自动扫描到执行的全流程 | 同样未完成，且执行桥接还需开发 |
| 使用成本 | 环节较少，适合局部实验 | 需管理规划服务、点云场景、附件包络及轨迹一致性 |

## 4. 原 Demo 当前的实际边界

固定姿态 `execute` 按当前代码检查片段长度 1–200 mm、表面法向与保持姿态的夹角小于 15°、沿保持法向的高度变化不超过 10 mm，接近距离现已改为可配置：默认不超过 500 mm，可用 `--max-approach-mm` 指定。这些是实验入口条件，不能替代整臂碰撞检查。本次按用户确认的无障碍可控实验条件，放宽悬空接近范围；仍保留速度、力、关节和跟踪保护，未做新的实机运动验证。

固定姿态适合局部浅曲面。`adaptive_execute` 提供随法向变化的姿态和补偿逻辑，但当前安装的重力标定尚未通过。返回原 Demo 不会消除这个限制。

力传感器原始单位已从 kgf / kgf·m 统一换算为 N / Nm。旧日志的力单位标注存在历史问题，不能把旧“1 N”目标直接当成实际 1 N 的验证依据。详见 [重力标定操作.md](重力标定操作.md)。

正常完成擦拭后，程序先离开曲面，再经预接触点返回启动悬空位姿；故障时停止，不自动清故障或盲目回位。

## 5. MoveIt 方案已经验证到哪一步

已完成的实测与软件验证：

- 独立 `/autowipe` 规划服务成功启动；没有连接机械臂硬件控制器，轨迹执行参数为 false。
- 用一份历史现场关节状态核对 FR3 法兰 FK：位置误差约 8.59×10⁻⁸ m，姿态误差约 1.13×10⁻⁵ 度。这是同一模型/位姿定义的一致性检查，不代表机械臂绝对物理精度。
- 182,965 个有效深度点生成 197 个观测体素，成功导入 MoveIt，并读回核对位置、姿态、数量和尺寸。
- 默认体素边长 20 mm，每侧加 10 mm 余量，实际碰撞盒边长为 40 mm。它是初始预览设置，不是已完成精度标定的最终配置。
- 离线图像重放串联入口成功运行，得到 6 个几何候选；路径审查停在末端附件碰撞几何缺失。
- 最近一次项目测试为 99 项通过。测试通过不代表整条现场运动路径或实机闭环验收通过。

证据文件：

- [模型核对结果](output/moveit_model_probe_tf.json)
- [体素导入与读回结果](output/depth_scene_verified/apply_result.json)
- [串联流程报告](output/autowipe_depth_replay/report.json)
- [路径审查阻塞原因](output/autowipe_depth_replay/motion_audit.json)

尚未完成：末端包络加载与核对、完整现场路径通过审查、未知空间覆盖约束、自动绕行回退、已验证关节轨迹执行、到位复扫及擦拭/返回闭环。

## 6. 不用 CAD，为什么仍有配置工作

RealSense 能提供可见表面的深度，所以工件曲面和可见环境不用手工建 CAD。当前实现将点云转成 CollisionObject 盒子集合；它不是原生 OctoMap，也没有把未观测区域编码成障碍。

末端的擦子、夹具、相机和传感器可能看不全，因此仍需要整体尺寸和相对法兰的安装位置。可以用尺量后配置简单包围盒，不需要精细 CAD。已有 TCP 偏移只描述工具参考点，不能表达整套附件占据的空间。

相机背面、遮挡处和无深度区域没有被证明为空闲。即使 MoveIt 返回一条在当前模型中无碰撞的路径，也不能据此宣称真实环境全程安全；后续必须结合多视角观测或可验证的受限运动区域。当前自动入口一直保持不可执行。

## 7. 当前回到原 Demo 后怎么使用

在项目目录运行：

```bash
cd /home/pnp/curve_wipe_demo
./run.sh preview
```

关闭预览释放相机后，可以使用原有自动几何预览：

```bash
./run.sh autowipe
```

这两条命令不使用 MoveIt，也不会移动机械臂。需要独立采集、指定区域规划时仍使用原 `capture`、`plan` 入口。对本次采集并生成的候选计划，可以先做固定姿态离线预检查：

```bash
./run.sh execute --plan <本次计划路径>
```

只有显式增加 `--execute` 才会连接硬件并进入运动流程，且仍需通过原有检查。不要使用 `autowipe` 的不可执行预览计划代替独立执行候选，也不要复用历史场景进行新一轮实机擦拭。

当前不启动 `planning`，不传 `--depth-scene` 或 `--motion-config`。MoveIt 扩展文件保留备用，原力控和运动入口未因本次切换而修改。

## 8. 当前选择的依据

当前先使用原 Demo，适合把目标附近的一段擦拭、接触力、传感器标定和正常回位验证清楚，调试环节较少。

以后如果需要从较远位置自动接近、在多块区域之间移动或绕过支架，再继续 MoveIt + RealSense 路线。它的价值是补充整臂路径规划；需要完成的工作是场景覆盖与执行链路，而不是要求你制作现场 CAD。

## 接近参数更新

原 `execute` 和 `adaptive_execute` 的 120 mm 硬编码门槛改为 `--max-approach-mm`，默认 500 mm；预检查输出和实机日志记录该上限，实机日志另记录实际接近距离。该距离为到预接触点的三维直线距离，不是最大工作高度。可显式传 `--max-approach-mm 120` 恢复旧上限。不会取消关节、力或跟踪检查，也没有修改接近速度、50 mm 预接触位置、10 mm 曲面起伏限制或接触搜索行程。

## 2026-09-13 更新：固定扫描起点与完整红条准备

原 Demo 新增 `./run.sh prepare_wipe`（只读检查）及 `./run.sh prepare_wipe --execute`（必要时悬空回到保存的初始位姿，然后重新扫描并生成整条红条预览）。不使用 MoveIt，不发送夹爪指令，也不直接开始接触擦拭。回位失败不会继续生成新计划。回位条件和实测范围详见 [文档.md](文档.md) 的“扫描初始位姿准备入口已接入”。

整条规划使用 `full_strip`，沿一个完整红条轮廓生成5条从端到端的路径，不再用“最长合格短片段”代表全部目标。深度空洞或曲面断裂会报告失败；白色印字不单独作为路径断点。已保存的完整规划见 [整条路径叠加图](output/full_strip_20260912/plan/overlay.png) 与 [规划摘要](output/full_strip_20260912/plan/summary.json)。该次每条长167–172 mm，总表面路径约845 mm；未核实擦子有效接触宽度与跨路径执行，因此不声明已完成整条实机覆盖。

## 最新阈值（2026-09-13）

当前固定姿态几何筛选已由10 mm起伏／15°法向夹角改为20 mm／25°，可通过 `execute --max-surface-height-mm` 与 `--max-normal-angle-deg` 配置。上文10 mm／15°是历史值。起伏沿固定工具法向计算，法向夹角表示曲面与擦子朝向的偏差，不是运动障碍物限制。新阈值未改变固定姿态控制方式，不能保证刚性擦面全幅贴合。最新5条路径有3条通过、2条仍被拒绝，未进行新的实机擦拭。

## 最新开发状态：端到端调度脚本

新增 [run_full_wipe.sh](run_full_wipe.sh) → [run_cycle.py](curve_wipe/run_cycle.py)，以逐条往返后回固定初始位姿的方式串联整条规划与原固定姿态执行器。每条前重新扫描、检查目标一致性，任何失败停止整轮。默认预检查，显式 `--execute` 才运动。详见 [端到端运行说明.md](端到端运行说明.md)。该软件链路已接入但尚未完成5条端到端实机验证；上文“尚无端到端入口”是之前的开发状态。

## 最新：擦子中心沿单条红条中心线，速度提高50%

端到端入口 `./run_full_wipe.sh --execute` 现默认1条中心线、`--speed-scale 1.5`。通过夹爪中心上方 15 mm 的擦拭中心反算末端位置。最新完整规划长约164.7 mm；沿线速度4.5 mm/s，悬空接近与返回15 mm/s。其它分阶段速度和历史默认值的区别见 [端到端运行说明.md](端到端运行说明.md)。本次只修改与离线验证，没有运动。

## 参考点调整：夹爪中心运动、擦子前端接触（2026-09-13）

原参数 `T_EE_L_TCP_eraser` 表示擦子接触中心。现已增加独立的 `T_EE_L_GRIP_CENTER` 和 `T_EE_L_WIPE_CENTER`：擦拭中心与夹爪中心 X/Y 重合，沿本地 Z 轴正方向偏移 15 mm。规划用擦拭中心对准红条，再反算夹爪中心对应的末端位姿；当前 `active_contact_reference=wipe_center`。
