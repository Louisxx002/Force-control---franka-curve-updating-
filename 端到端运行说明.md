# 端到端整条擦拭入口

**最新设置：单条当前可见目标胶带中心线，使用夹爪中心上方 15 mm 的擦拭中心；速度为原 Demo 的1.5倍。** 画面外或被遮挡的部分不纳入本次擦拭区域。默认不再跑5条路径。夹爪/法兰的命令位置由擦拭中心偏移反算。

工具参考点已拆分为 `T_EE_L_GRIP_CENTER`（夹爪中心）和 `T_EE_L_WIPE_CENTER`（夹爪中心沿本地 Z 轴正方向 15 mm）。当前启用 `active_contact_reference=wipe_center`；轨迹以该擦拭中心对准红条，夹爪中心位姿由变换反算得到。

当前入口：[run_full_wipe.sh](run_full_wipe.sh)，调度代码：[curve_wipe/run_cycle.py](curve_wipe/run_cycle.py)。默认目标是红色胶带；支持红、橙、黄、黄绿、绿、青、蓝、紫、品红、粉、棕、灰、白、黑等颜色，可用 `--target-color <color>` 选择。工具安装不变、无障碍的可控实验。保持相机预览关闭以释放 RealSense。

```bash
cd /home/pnp/curve_wipe_demo
# 默认：在初始位姿新扫描并预检查，不发送运动命令
./run_full_wipe.sh
# 实机：必要时回初始位姿，扫描规划，整条中心线往返擦拭并返回
./run_full_wipe.sh --execute
# 白色胶带
./run_full_wipe.sh --execute --target-color white
# 黑色胶带
./run_full_wipe.sh --execute --target-color black
```

可显式指定筛选参数：

```bash
./run_full_wipe.sh --execute --lanes 1 \
  --max-approach-mm 500 \
  --max-surface-height-mm 30 \
  --max-normal-angle-deg 45
```

离线复现，不读取硬件：

```bash
./run_full_wipe.sh --snapshot output/wipe_return_trial_20260913/scan/snapshot.npz
```

`--snapshot` 与 `--execute` 互斥。脚本不会从历史图像执行新运动。各次输出使用时间戳目录，也可传 `--output <新目录>`。

## 实际执行顺序

1. 读取保存的 `config/scan_initial_pose.json`。不在参考位姿时，先新扫描并检查悬空回位条件，再回位。已到位则不重复移动。
2. 新扫描并规划当前画面中唯一连续可见的目标胶带，默认1条可见段中心线。画面外或被工具遮挡的部分视为不存在，不推断其位置；可见段仍必须满足颜色识别、深度、几何和接近条件。
3. 所有路径使用第2步这一次扫描生成的计划；每条执行前只确认机器人仍在初始位姿，不再重复采集可能出现坏帧的深度图。
4. 当前路径按1 N目标力往返各一次，离面、经预接触点返回本次启动悬空位姿，再核对它与保存的固定初始位姿一致。
5. 回位确认通过后才开始下一条。擦拭器先回到本轮起始位姿；若与保存的扫描初始位姿仅相差不超过 5 mm / 5°，再执行一次受限的空中末端校正，校正后重新核对，避免毫米级末端跟踪误差被误判为整条轨迹失败。偏差超出该范围仍立即停止。全部完成后保存末次图像作为观察记录。不根据红色是否仍存在无限重试，胶带本身不假定可以擦除。

各条间经过悬空初始位姿，不在接触状态下直接横移换行。因此耗时会明显长于只擦一条短路径。

## 放宽范围

| 项目 | 本端到端入口默认值 | 备注 |
|---|---:|---|
| 曲面起伏 | 30 mm | 旧独立 `execute` 默认仍为20 mm；本入口显式传入30 mm |
| 曲面法向夹角 | 45° | 旧独立入口默认仍为25°；端到端执行逐点跟随空间法向 |
| 悬空接近距离 | 500 mm | CLI可配置 |
| 准备阶段回位TCP平移 | 跟随接近参数，默认500 mm | 原独立 `prepare_wipe` 默认仍保留180 mm |
| 准备阶段回位旋转 | 45° | 本入口显式传入，原独立回位默认25° |
| 擦拭前悬空阈值 | 0.75 N | 保持现有值 |
| 目标力 | 1 N | 保持现有值 |

仍保留：回位前相对观测曲面100 mm离面条件、慢速搜索范围、合力8 N、法向力8 N的力保护、力矩1.5 Nm保护、关节2、跟踪、传感器和循环超时、故障停止。正向与反向擦拭之间增加0.5 s定点过渡，曲面法向平滑窗口为20 mm，以避免反向时的速度/加速度不连续。空间擦拭姿态由每个轨迹点的实测法向重建，夹爪 TCP +Z 指向曲面内侧（即 `TCP_Z = -normal_out`）；工具标定旋转用于把 TCP 姿态换算成末端姿态。故障时不盲目回位、不清故障、不自动重试。

## 执行资格与结果含义

可见段规划文件仍保持预览标记，旧 `execute --plan` 不会直接接受它。端到端入口通过单独的验证接口检查可见段完整性、图像哈希、标定哈希与位姿定义后，按明确的路径编号调用执行器；没有靠改预览文件标记来运行。

`cycle_report.json` 记录当前条、已完成条和逐条回位误差；每条目录包含新扫描、规划及 `wipe.json`。`completed_all_lanes_and_returned` 表示所有轨迹运动与回位完成，不代表痕迹去除率或擦子全幅接触已经测量。擦拭阶段每个 10 ms 控制周期按弧长插值曲面法向并重建 TCP 姿态，法向力误差通过导纳调节接触高度。

离线真实图像验证：5条路径全部通过30 mm／45°筛选，每条约164–166 mm，接近约297–350 mm，报告见 [cycle_offline_20260913/cycle_report.json](output/cycle_offline_20260913/cycle_report.json)。本次没有进行5条端到端实机运行，不能把离线通过当作实机验收。

## 单中心线与速度更新

`./run_full_wipe.sh --execute` 默认 `--lanes 1 --speed-scale 1.5`。目标胶带可见连通区域的上下边界中点生成中心线，白字不切断轨迹。红色目标要求可见中心线深度完整；白色或黑色胶带模式会用上下相邻的有效曲面点重建缺失中心点，局部拟合残差超过 3 mm 仍拒绝执行。遮挡段和出框段不纳入中心线。黑色胶带允许图像中的斜向长条，并按主轴生成中心线。现有 `config/eraser_tcp_candidate.json` 定义的擦拭中心沿曲面中心线移动，夹爪中心与其保持 15 mm 的本地 Z 轴偏移。

| 运动阶段 | 原速度 | 现速度 |
|---|---:|---:|
| 悬空接近／擦拭后回启动点 | 10 mm/s | 15 mm/s |
| 近面过渡／离面退出 | 5 mm/s | 7.5 mm/s |
| 慢速接触搜索 | 1 mm/s | 1.5 mm/s |
| 沿红条擦拭 | 3 mm/s | 4.5 mm/s |
| 准备阶段回参考位置 | 8 mm/s | 12 mm/s |
| 准备阶段转姿态 | 2°/s | 3°/s |

擦拭段现在采用 0.5 s 的有限加加速度起步和收尾，中间保持设定匀速；悬空接近、离面和回位使用 Franky `CartesianWaypoint` 的 `minimum_time` 约束每段运动时间，避免单次位姿指令按默认速度突然加速。控制采样周期、力控增益/法向修正速度、停稳时间、超力与跟踪阈值不变。独立 `execute` 命令仍沿用旧速度，端到端入口显式传入1.5倍。

已用保存图像生成一条约164.7 mm的完整中心线并通过预检查：[叠加图](output/centerline_speed150_preview/plan/overlay.png)。旧5条方案与日志作为历史记录保留。本次未运行实机增速测试。

## PNP75 接触导纳控制

端到端入口的悬空段不使用力样本；只有到达预接触点才采集静止基线并启用 PNP75。擦拭阶段采用局部曲面法向对齐的虚拟导纳，力误差调节法向位置，目标1 N。空中不因短暂串口帧延迟而停止，但接触阶段必须有新鲜完整帧；传感器中断会停止本条，不使用旧值继续擦。
