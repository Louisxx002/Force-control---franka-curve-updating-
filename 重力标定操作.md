# 当前工具重力标定

## 2026-09-12 单位修正及本次数据结论

用户确认当前 PNP75 输出力为 kg（按 kgf 换算）、力矩为 kgm（按 kgf·m 换算）。
串口及 SDK 接口现统一乘以 9.80665，输出 N / Nm；下游控制器不再乘第二次。
之前日志中的 N/Nm 标签不正确，旧“1 N”目标数值对应约 9.81 N 的传感器力分量，
不能作为已实现实际 1 N 接触力的证据。

原始 `data/payload_calibration/session.json` 保留不变；SI 副本为 `session_si.json`，
保留原始文件 SHA256、时间戳及机器人状态，仅转换传感器均值、标准差、六维时间序列。
禁止继续向未标记单位的旧会话追加数据。现有转换脚本会拒绝重复转换。

重算诊断：等效负载约 1.111 kg；验证组残余力 RMS 0.656 N、最大 0.800 N；
模型一致性比例 1.639，超过 1.5 上限。尚未通过标定，未生成可执行补偿配置。
报告：`output/gravity_sampling_diagnostic_si.json`。该诊断中的力矩残差在传感器原点计算，
不代表测量原点相对 EE 的位置已确定。

下一次采样先检查线缆余量、工具是否完全松手且停稳，使用新文件：

```bash
./run.sh calibrate_payload collect --output data/payload_calibration/session_si_retry.json
```

接口修正后，采样稳定性阈值才实际对应 0.05 N / 0.005 Nm；不放宽阈值来通过旧数据。
新采样拟合时显式指定 `--samples data/payload_calibration/session_si_retry.json`。
旧会话仅在完成来源核对后才能用 `convert_payload_units` 转换，不能直接按 N/Nm 拟合。

脚本不发送机械臂运动命令、不恢复故障、不修改负载或EE配置、不对传感器执行清零。
只调用现有 `read_state` 读取位姿，用实际擦拭同款串口解析器读取六维力。

## 第一步：准备

保持相机、传感器、夹爪、黑板擦安装不变。把工具放到开阔悬空位置，保证各个倾角下都不碰桌面、曲面或其他物体。
不要扶着黑板擦采样；线缆不能绷紧或蹭环境。关闭其他力传感器程序及擦拭程序。

手动引导到目标姿态后松开按钮与工具，等约2秒停稳，确保FCI可读，再采样。
手动引导/释放按钮由操作者完成；本程序不会替你切换机器人模式。

## 第二步：运行交互采样

```bash
cd /home/pnp/curve_wipe_demo
./run.sh calibrate_payload collect
```

确认悬空后输入 `YES`。每调整好一个姿态，按一次 Enter；每组采集约2秒。
默认先16组拟合、后6组独立验证。输入 `q` 保存退出；下次同一命令会接着采。
安装或工件承载改变后不要续用旧会话，可指定新文件：

```bash
./run.sh calibrate_payload collect --output data/payload_calibration/session_new.json
```

姿态安排：以将来擦拭的大致姿态为中心，分别向前、后、左、右倾斜，再增加不同程度的组合倾斜。
在有足够空间和关节余量的情况下覆盖约正负20–30°；这只是采样建议，不是程序自动执行的角度。
不要仅平移，不要只围绕竖直方向转动，也不要只绕单一轴倾斜。
脚本拒绝与已有重力方向相差不足3°的重复姿态，拟合还会检查三维方向激励。

最后6组使用未参与拟合的新倾角。采样时仍完全悬空，不接触曲面。
如果本组失败，屏幕说明原因，该组不会写入有效样本；排除原因后重新按Enter。
每组有效数据立即落盘，保留原始六维时间序列、前/中/后机器人状态。

## 第三步：填写一个实测安装尺寸

需要传感器“测量原点”在当前EE坐标系下的位置 `[X,Y,Z]`，单位mm。
方向是 **从EE原点指向传感器测量原点，并用EE的XYZ轴表示**。
测量原点应查传感器机械图纸；不能把传感器外壳中心随意当作测量原点。
当前EE通常不在机械臂法兰，需要把法兰到EE的偏移计入。
传感器旋转由脚本用多姿态重力数据估计；平移无法通过悬空重力辨识。
暂时不知道这三个数时，先完成第二步，保留数据，再核对安装尺寸。不要猜成0。

## 第四步：离线拟合和独立验证

把下面 X_MM、Y_MM、Z_MM 替换为刚才实测的三个数字：

```bash
./run.sh calibrate_payload fit --sensor-origin-ee-mm X_MM Y_MM Z_MM
```

默认读取 `data/payload_calibration/session.json`，输出 `config/gravity_calibration.json`。
如果用了新会话，增加 `--samples data/payload_calibration/session_new.json`。

输出包含质量、安装旋转、传感器符号、质心、六维零偏及拟合/验证残余力。
通过条件包含：至少12个训练姿态、5个独立验证姿态、足够的三维重力方向变化、
训练和验证力矢量RMS均<=0.1N、单姿态力矢量残余<=0.2N、力矩残余<=0.03Nm。
这些是本demo的初步接触力检查标准，不是计量精度认证。

只有全部通过才输出 `validated_current_mount`。失败结果仅为候选，不能用于新版变姿态擦拭。
不要手改状态字段。标定有效性还依赖操作者确实悬空采样、尺寸正确、安装保持不变。

本标定是静态模型，不补偿工具加速度、线缆牵拉、温漂或松动。新执行入口仍会在实际运行前检查悬空残余力。

先把采样做完，把最后的拟合输出发给我；接着检查结果和新版首点悬空姿态，再做1N短距离变姿态测试。
